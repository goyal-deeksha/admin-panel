import { Container } from 'react-bootstrap';
import { Teams } from 'sub-components';

const Profile = () => {
    return (
        <Container fluid className="p-6">
            <Teams />
        </Container>
    )
}

export default Profile